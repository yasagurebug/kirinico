<Window x:Class="Kirinico.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About"
        Width="420"
        Height="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#101316"
        Foreground="#E8EEF5">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Text="{Binding AppExecutableName}" />

        <TextBlock Grid.Row="1"
                   Margin="0,12,0,0"
                   Text="{Binding AppVersion, StringFormat=Version {0}}" />

        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   Text="{Binding AuthorName, StringFormat=Author: {0}}" />

        <StackPanel Grid.Row="3"
                    Margin="0,12,0,0"
                    Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center"
                       Text="GitHub: " />
            <TextBlock VerticalAlignment="Center">
                <Hyperlink NavigateUri="{Binding RepositoryUrl, Mode=OneWay}"
                           RequestNavigate="RepositoryLink_OnRequestNavigate">
                    <Run Text="{Binding RepositoryUrl, Mode=OneWay}" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>

        <Button Grid.Row="5"
                Width="96"
                Margin="0,16,0,0"
                HorizontalAlignment="Right"
                Padding="12,6"
                Click="CloseButton_OnClick"
                Content="閉じる" />
    </Grid>
</Window>
